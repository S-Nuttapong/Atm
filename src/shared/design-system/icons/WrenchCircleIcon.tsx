import { WrenchIcon } from '@shared/design-system/icons/WrenchIcon'
import { createCircleIcon } from '@shared/design-system/icons/createCircleIcon'

export const WrenchCircleIcon = createCircleIcon(WrenchIcon)
