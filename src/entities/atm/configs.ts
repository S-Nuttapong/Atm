import { Transaction } from "@shared/types";

const allTransactions: Transaction[] = [
    'EnterPin',
    'MoneyTransfer',
    'ViewBalance',
    'WithdrawCash',
    'TransactionsMenu',
    'ChangePin'
]

export const atmConfigs = {
    allTransactions
}